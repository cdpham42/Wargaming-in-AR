using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using GameUnit;

public class Board : MonoBehaviour
{

    //public SyncUnit unitPrefab;

    //List<SyncUnit> Units = new List<SyncUnit>();

    private void Awake()
    {
        //SyncUnit.unitPrefab = unitPrefab;
    }

    public void RemoveUnit(SyncUnit unit)
    {
        //Units.Remove(unit);
        //unit.Die();
    }

    // Update is called once per frame

    void Update()
    {



    }
}
